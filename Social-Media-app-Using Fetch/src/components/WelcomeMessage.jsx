const WelcomeMessage=({handlePostData})=>{
    return(<center  className="welcome-message"><h1>There is no post</h1>
 
            {/* <button type="button" className="btn btn-primary"  onClick={handlePostData}>Get post from server</button> */}
    
    
    </center>)
}

export default WelcomeMessage;